# Text normalization: the authored content of this repository is Markdown
# and stays LF.
* text=auto eol=lf

# Figure assets are stored verbatim. SVG is XML, but this one is a generated
# export, so end-of-line normalization is disabled to keep the bytes stable.
*.svg -text
